name: Automated Airtel Tv
on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  update-file:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          
      - name: Configure Git User
        run: |
          git config --global user.name "sportlive18"
          git config --global user.email "271363488+sportlive18@users.noreply.github.com"
          
      - name: Sync and Update Log
        run: |
          export TZ="Asia/Kolkata"
          git pull origin main --rebase
          curl -sS -L -o airteltv.m3u "http://airteltataplaygeorolexmix.gojo45749.workers.dev"
          echo "🚀 JIOTV  Synced | IST: $(date +'%Y-%m-%d %H:%M:%S')"
          
      - name: Commit and Push
        run: |
          export TZ="Asia/Kolkata"
          git add airteltv.m3u 
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "JIOTV Updated ✅ [$(date +'%d-%m-%Y %H:%M')]"
          git push origin main
